{
  "name": "daily-learning-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute daily-learning-db --local --file=./schema.sql",
    "db:migrate:prod": "wrangler d1 execute daily-learning-db --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260127.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.61.0"
  }
}
